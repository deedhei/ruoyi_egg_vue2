"use strict";

const rule = {
  // id: [{ required: false, message: "用户名不能为空" }],
  // passResult: [{ required: false, message: "用户名不能为空" }],
  // cardVideo: [{ required: false, message: "用户名不能为空" }],
  // cardImage: [{ required: false, message: "用户名不能为空" }],
  // cardInfoOcrJson: [{ required: false, message: "用户名不能为空" }],
  // requestId: [{ required: false, message: "用户名不能为空" }],
  // sdkVerificationId: [{ required: false, message: "用户名不能为空" }],
  // createTime: [{ required: false, message: "用户名不能为空" }],
};

module.exports = rule;
